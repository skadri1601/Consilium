name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  claude-review:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
    env:
      ANTHROPIC_MODEL: claude-sonnet-4-20250514
      ANTHROPIC_DEFAULT_SONNET_MODEL: claude-sonnet-4-20250514
      ANTHROPIC_DEFAULT_HAIKU_MODEL: claude-haiku-4-5-20251001

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        # The claude-code-action is a nice-to-have review bot, not a gating
        # check. It has been failing consistently across PRs (likely a plugin-
        # marketplace or ANTHROPIC_API_KEY issue that needs repo-secret
        # inspection). Step-level continue-on-error so PRs aren't blocked on
        # it until a maintainer verifies the secret + marketplace reachability.
        continue-on-error: true
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          plugin_marketplaces: 'https://github.com/anthropics/claude-code.git'
          plugins: 'code-review@claude-code-plugins'
          claude_args: '--model sonnet --fallback-model haiku'
          prompt: '/code-review:code-review ${{ github.repository }}/pull/${{ github.event.pull_request.number }}'
