# ============================================
# Consilium - Environment Variables Template
# ============================================
# Copy this file to .env.local and fill in your actual values
# Windows: Copy-Item env.example.txt .env.local
# macOS/Linux: cp env.example.txt .env.local

# ============================================
# Database (PostgreSQL)
# ============================================
# Option 1: Use Docker Compose (recommended for local dev)
# Set POSTGRES_PASSWORD in your .env.local (required - use a strong password!)
# DATABASE_URL="postgresql://consilium:${POSTGRES_PASSWORD}@localhost:5432/consilium"
POSTGRES_PASSWORD="change-this-to-a-strong-password"
#
# Option 2: Use Neon PostgreSQL (for production/demo)
# Get from: https://console.neon.tech → Project → Connect
DATABASE_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
DIRECT_DATABASE_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# ============================================
# Redis
# ============================================
# Option 1: Use Docker Compose (recommended for local dev)
# REDIS_URL="redis://localhost:6379"
#
# Option 2: Use Upstash Redis (for production/demo)
# Get from: https://console.upstash.com → Database → Details
UPSTASH_REDIS_REST_URL="https://xxx.upstash.io"
UPSTASH_REDIS_REST_TOKEN="AXxxxxxxxxxxxx"
UPSTASH_REDIS_URL="https://xxx.upstash.io"
UPSTASH_REDIS_TOKEN="AXxxxxxxxxxxxx"
REDIS_URL="rediss://default:xxx@xxx.upstash.io:6379"

# ============================================
# LLM Provider API Keys
# ============================================
# These are for the demo instance. Users can also provide their own keys (BYOK)
# via the settings page in the app.
#
# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# Anthropic: https://console.anthropic.com
ANTHROPIC_API_KEY="sk-ant-..."

# Google AI: https://aistudio.google.com/apikey
GOOGLE_API_KEY="AIza..."

# Groq: https://console.groq.com/keys
GROQ_API_KEY="gsk_..."

# X.AI (Grok): https://console.x.ai (optional)
XAI_API_KEY="xai-..."

# ============================================
# Authentication (Clerk)
# ============================================
# Get from: https://dashboard.clerk.com → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."

# ============================================
# Note: Stripe removed - this is an open source project
# Users bring their own API keys (BYOK) or use hosted keys below
# ============================================

# ============================================
# Monitoring (Sentry)
# ============================================
# Get from: https://sentry.io → Project Settings → Client Keys
SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"
SENTRY_AUTH_TOKEN=""
SENTRY_ORG="consilium-pi"
SENTRY_PROJECT="javascript-nextjs"

# ============================================
# Email (Resend)
# ============================================
# Get from: https://resend.com/api-keys
RESEND_API_KEY="re_..."

# ============================================
# Application URLs
# ============================================
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:4000"
FRONTEND_URL="http://localhost:3000"
CORS_ORIGINS="http://localhost:3000,http://localhost:4000"

# ============================================
# Backend API Configuration
# ============================================
NODE_ENV="development"
PORT="4000"
API_PREFIX="api"

# ============================================
# AI Workers Configuration
# ============================================
BACKEND_API_URL="http://localhost:4000"

# ============================================
# Security
# ============================================
JWT_SECRET="your-32-character-secret-here-change-in-production"

# Encryption key for API keys stored in database (32 bytes / 64 hex chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"

# Internal webhook secret for service-to-service communication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_WEBHOOK_SECRET="your-internal-webhook-secret-here"

# Clerk webhook secret (from Clerk Dashboard → Webhooks)
CLERK_WEBHOOK_SECRET="whsec_..."

# ============================================
# Langfuse (Observability - Optional)
# ============================================
LANGFUSE_SECRET_KEY=""
LANGFUSE_PUBLIC_KEY=""
LANGFUSE_HOST="https://cloud.langfuse.com"

